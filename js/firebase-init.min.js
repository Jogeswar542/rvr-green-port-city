import{initializeApp}from 'https:import{getAuth,signInAnonymously,GoogleAuthProvider,signInWithPopup}from 'https:import{getFirestore,enableIndexedDbPersistence,addDoc,collection,serverTimestamp,onSnapshotsInSync}from 'https:const firebaseConfig ={apiKey:"AIzaSyBEwVpoK8P5WrrJwucvCRcf-MvYQTSS6SQ",authDomain:"rvr-green-port-city.firebaseapp.com",projectId:"rvr-green-port-city",storageBucket:"rvr-green-port-city.firebasestorage.app",messagingSenderId:"683382674368",appId:"1:683382674368:web:a820ff8240cbd6061fae25",measurementId:"G-LP0K2ZE6Z8"};const app = initializeApp(firebaseConfig);export const db = getFirestore(app);export const auth = getAuth(app);let authReady = false;export async function ensureAuth(){if(authReady && auth.currentUser)return auth.currentUser;try{const cred = await signInAnonymously(auth);authReady = true;console.log('✅ Anonymous auth active');return cred.user;}catch(err){console.warn('⚠️ Anonymous auth failed:',err);throw err;}}try{await enableIndexedDbPersistence(db);console.log('✅ Firestore offline persistence enabled');}catch(err){if(err.code === 'failed-precondition'){console.warn('⚠️ Multiple tabs open:persistence only in one tab');}else if(err.code === 'unimplemented'){console.warn('⚠️ Browser does not support offline persistence');}else{console.warn('⚠️ Persistence error:',err);}}let syncStatus = navigator.onLine ? 'synced':'offline';let plotsSyncStatus = 'offline';onSnapshotsInSync(db,()=>{syncStatus = navigator.onLine ? 'synced':'offline';updateSyncIndicator();updatePlotsSyncIndicator();});function updateSyncIndicator(){const el = document.getElementById('syncIndicator');if(!el)return;el.style.display = 'block';if(syncStatus === 'synced'){el.textContent = '🟢 Synced';el.style.background = '#10b981';setTimeout(()=>{el.style.display = 'none';},2500);}else{el.textContent = '🔴 Offline - Will sync later';el.style.background = '#ef4444';}}export function updatePlotsSyncIndicator(){const el = document.getElementById('plotsSyncIndicator');if(!el)return;el.style.display = 'block';if(plotsSyncStatus === 'synced'){el.textContent = '🟢 Plots Synced';el.style.background = '#10b981';setTimeout(()=>{el.style.display = 'none';},2500);}else if(plotsSyncStatus === 'offline'){el.textContent = '📦 Plots Offline Cache';el.style.background = '#6366f1';}else{el.textContent = '🟡 Syncing Plots...';el.style.background = '#f59e0b';}}export function setPlotsSyncStatus(status){plotsSyncStatus = status;updatePlotsSyncIndicator();}window.addEventListener('online',()=>{syncStatus = 'synced';updateSyncIndicator();if(window.onOnlineRestore)window.onOnlineRestore();});const AUTHORIZED_USERS ={admin:[ 'admin@rvrgreenporcity.com','rvradmin@gmail.com','suntraderskvl@gmail.com','jogebablu9700@gmail.com','kalyanraju532@gmail.com','rvrconsultancy2003@gmail.com',],agent:[ 'agent@rvrgreenporcity.com','rvragent@gmail.com','suntraderskvl@gmail.com','jogebablu9700@gmail.com','kalyanraju532@gmail.com','rvrconsultancy2003@gmail.com',]};export async function signInWithGoogle(requestedRole){try{const provider = new GoogleAuthProvider();provider.setCustomParameters({prompt:'select_account'});const result = await signInWithPopup(auth,provider);const user = result.user;const email = user.email.toLowerCase();console.log('✅ Google Sign-In successful:',email);const roleEmails = AUTHORIZED_USERS[requestedRole] || [];const normalizedEmails = roleEmails.map(e => e.toLowerCase());if(!normalizedEmails.includes(email)){let actualRole = null;for(const [role,emails] of Object.entries(AUTHORIZED_USERS)){if(emails.map(e => e.toLowerCase()).includes(email)){actualRole = role;break;}}if(actualRole){throw new Error(`This email is authorized as ${actualRole.toUpperCase()},not ${requestedRole.toUpperCase()}`);}else{throw new Error('This email is not authorized. Please contact administrator.');}}const session ={email:email,role:requestedRole,name:user.displayName || email.split('@')[0],photoURL:user.photoURL,loginTime:Date.now(),expiresAt:Date.now()+(24 * 60 * 60 * 1000)};localStorage.setItem('userSession',JSON.stringify(session));localStorage.setItem('userRole',requestedRole);return{success:true,role:requestedRole,user:session};}catch(error){console.error('❌ Google Sign-In failed:',error);if(error.code === 'auth/popup-closed-by-user'){throw new Error('Sign-in cancelled');}else if(error.code === 'auth/popup-blocked'){throw new Error('Popup blocked. Please allow popups and try again.');}else if(error.message.includes('not authorized')){throw error;}else{throw new Error('Sign-in failed:' + error.message);}}}export function isEmailAuthorized(email){const normalizedEmail = email.toLowerCase();for(const emails of Object.values(AUTHORIZED_USERS)){if(emails.map(e => e.toLowerCase()).includes(normalizedEmail)){return true;}}return false;}export function getEmailRole(email){const normalizedEmail = email.toLowerCase();for(const [role,emails] of Object.entries(AUTHORIZED_USERS)){if(emails.map(e => e.toLowerCase()).includes(normalizedEmail)){return role;}}return null;}export const PASSCODES ={admin:'admin123',agent:'agent123'};export async function signInWithPasscode(requestedRole,passcode){const expected = PASSCODES[requestedRole];if(!expected)throw new Error('Unknown role');if(passcode !== expected)throw new Error('Invalid passcode');const session ={email:`${requestedRole}@passcode.local`,role:requestedRole,name:`${requestedRole.toUpperCase()}User`,photoURL:null,loginTime:Date.now(),expiresAt:Date.now()+(24 * 60 * 60 * 1000)};localStorage.setItem('userSession',JSON.stringify(session));localStorage.setItem('userRole',requestedRole);return{success:true,role:requestedRole,user:session};}export const AGENT_ACCOUNTS ={'agent001':{password:'Rvr@Agent2026',name:'Default Agent'},'kalyan':{password:'Kalyan@532',name:'Kalyan Raju'},'joge':{password:'Joge@9700',name:'Jogebabu'},'suntraders':{password:'Sun@2026',name:'Sun Traders KVL'},'rvrconsult':{password:'Rvr@2003',name:'RVR Consultancy'},};export async function signInAgentWithCredentials(agentId,password){const rec = AGENT_ACCOUNTS[agentId];if(!rec)throw new Error('Unknown Agent ID');if(rec.password !== password)throw new Error('Incorrect password');await ensureAuth();const session ={email:`${agentId}@agent.local`,role:'agent',name:rec.name || agentId,photoURL:null,loginTime:Date.now(),expiresAt:Date.now()+(24 * 60 * 60 * 1000)};localStorage.setItem('userSession',JSON.stringify(session));localStorage.setItem('userRole','agent');return{success:true,role:'agent',user:session};}export const ADMIN_ACCOUNTS ={'admin001':{password:'Rvr@Admin2026',name:'Default Admin'},'rvradmin':{password:'Rvr@Admin2026',name:'RVR Admin'},};export async function signInAdminWithCredentials(adminId,password){const rec = ADMIN_ACCOUNTS[adminId];if(!rec)throw new Error('Unknown Admin ID');if(rec.password !== password)throw new Error('Incorrect password');await ensureAuth();const session ={email:`${adminId}@admin.local`,role:'admin',name:rec.name || adminId,photoURL:null,loginTime:Date.now(),expiresAt:Date.now()+(24 * 60 * 60 * 1000)};localStorage.setItem('userSession',JSON.stringify(session));localStorage.setItem('userRole','admin');return{success:true,role:'admin',user:session};}window.addEventListener('offline',()=>{syncStatus = 'offline';plotsSyncStatus = 'offline';updateSyncIndicator();updatePlotsSyncIndicator();});window.onOnlineRestore = async()=>{if(!window.getOfflineEnquiries || !window.markEnquirySynced){console.warn('⚠️ Local storage module not loaded yet');return;}try{const offlineEnquiries = await getOfflineEnquiries();if(offlineEnquiries.length === 0){console.log('✅ No offline enquiries to sync');return;}console.log(`🔄 Syncing ${offlineEnquiries.length}offline enquiries...`);let synced = 0;for(const enquiry of offlineEnquiries){try{const docRef = await addDoc(collection(db,'enquiries'),{...enquiry,synced:true,syncedAt:serverTimestamp(),});await markEnquirySynced(enquiry.id || enquiry.timestamp);synced++;}catch(err){console.warn(`⚠️ Failed to sync enquiry:`,err);}}console.log(`✅ Synced ${synced}/${offlineEnquiries.length}enquiries`);const indicator = document.getElementById('syncIndicator');if(indicator){indicator.textContent = `✓ Synced ${synced}enquiries`;indicator.style.background = '#10b981';indicator.style.display = 'block';setTimeout(()=>{indicator.style.display = 'none';},3000);}}catch(err){console.error('❌ Error during offline sync:',err);}};window.submitEnquiryOffline = async(event)=>{event.preventDefault();const form = event.target;const submitBtn = form.querySelector('button[type="submit"]');const originalText = submitBtn ? submitBtn.textContent:'Send Enquiry Now';const formData = new FormData(form);const data = Object.fromEntries(formData);const payload ={...data,status:'new',submittedAt:new Date().toISOString(),timestamp:serverTimestamp(),offline:!navigator.onLine,};try{if(submitBtn){submitBtn.disabled = true;submitBtn.textContent = navigator.onLine ? 'Submitting…':'Saving offline…';}const ref = await addDoc(collection(db,'enquiries'),payload);console.log('✅ Enquiry saved:',ref.id);alert(navigator.onLine ? 'Thank you! Your enquiry has been submitted. Our team will contact you within 30 minutes.':'Saved offline! It will sync automatically when you are online.');form.reset();}catch(err){console.error('❌ Error saving enquiry:',err);alert('Error submitting enquiry. Please try again.');}finally{if(submitBtn){submitBtn.disabled = false;submitBtn.textContent = originalText;}}};