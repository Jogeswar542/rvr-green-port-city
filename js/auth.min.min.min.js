import{collection,onSnapshot,doc,updateDoc}from "https:import{db,setPlotsSyncStatus,ensureAuth}from "./firebase-init.js";let lastSyncTime = null;async function cachePlotsOffline(plots){try{if(window.cachePlotsLocally){await window.cachePlotsLocally(plots);lastSyncTime = new Date();updateSyncTimestamp();console.log('✅ Plots cached in IndexedDB:',plots.length,'plots');}else{localStorage.setItem('rvr_plots_cache',JSON.stringify(plots));localStorage.setItem('rvr_plots_timestamp',new Date().toISOString());lastSyncTime = new Date();updateSyncTimestamp();}}catch(e){console.warn('⚠️ Could not cache plots:',e.message);}}async function getPlotsFromCache(){try{if(window.getCachedPlots){const cached = await window.getCachedPlots();if(cached && cached.length > 0){const timestamp = localStorage.getItem('rvr_plots_timestamp');lastSyncTime = timestamp ? new Date(timestamp):null;updateSyncTimestamp();}return cached;}else{const cached = localStorage.getItem('rvr_plots_cache');if(cached){const timestamp = localStorage.getItem('rvr_plots_timestamp');lastSyncTime = timestamp ? new Date(timestamp):null;updateSyncTimestamp();return JSON.parse(cached);}return null;}}catch(e){console.warn('⚠️ Could not read plots cache:',e.message);return null;}}function updateSyncTimestamp(){const indicator = document.getElementById('plotsSyncIndicator');if(!indicator || !lastSyncTime)return;const now = new Date();const diffMs = now - lastSyncTime;const diffMins = Math.floor(diffMs / 60000);let timeAgo = '';if(diffMins < 1)timeAgo = 'just now';else if(diffMins < 60)timeAgo = `${diffMins}m ago`;else if(diffMins < 1440)timeAgo = `${Math.floor(diffMins/60)}h ago`;else timeAgo = `${Math.floor(diffMins/1440)}d ago`;const syncText = navigator.onLine ? `🟢 Synced ${timeAgo}`:`📦 Offline(synced ${timeAgo})`;indicator.textContent = syncText;}export function startRealtimeSync(onDataUpdate){ensureAuth().catch((e)=>console.warn('Auth not ready for sync',e));getPlotsFromCache().then(cached =>{if(cached && cached.length > 0){window.allPlots = cached;setPlotsSyncStatus('syncing');if(onDataUpdate)onDataUpdate(cached);console.log('⚡ Loaded from cache:',cached.length,'plots(syncing in background)');}});const plotsRef = collection(db,"plots");const unsubscribe = onSnapshot(plotsRef,{includeMetadataChanges:false},(snapshot)=>{if(snapshot.metadata.fromCache){console.log('📦 From Firestore cache');return;}const plots = snapshot.docs.map(doc =>({id:doc.id,...doc.data()}));window.allPlots = plots;cachePlotsOffline(plots);setPlotsSyncStatus('synced');if(onDataUpdate)onDataUpdate(plots);console.log('✅ Synced fresh plots:',plots.length);},(error)=>{console.error("Sync Error:",error);getPlotsFromCache().then(cached =>{if(cached && cached.length > 0){console.log('⚠️ Using cached plots(offline mode):',cached.length,'plots');window.allPlots = cached;setPlotsSyncStatus('offline');if(onDataUpdate)onDataUpdate(cached);}});});setInterval(updateSyncTimestamp,60000);return unsubscribe;}window.onOnlineRestore =()=>{console.log('🔄 Reconnected - syncing plots...');setPlotsSyncStatus('syncing');updateSyncTimestamp();};export async function updatePlotInCloud(plotId,data){if(!plotId)return false;const plotRef = doc(db,"plots",plotId);try{await updateDoc(plotRef,{status:data.status,pricePerAnkanam:data.price,totalPrice:data.total,remarks:data.remarks,lastUpdated:new Date().toISOString()});return true;}catch(e){console.error("Update Error:",e);alert("Failed to save:" + e.message);return false;}}export function logout(){sessionStorage.clear();window.location.href = "login.html";}